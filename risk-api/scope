Full name (Purpose):
A data-driven ESG (Environmental, Social, Governance) investment analytics API that helps portfolio managers and ESG analysts query company metrics, analyze performance, and make informed decisions.

## Why did I pursue in such project?
- Pain: No centralised API to query company ESG metrics + financial data + portfolio-level KPIs
- Client: Portfolio manager / ESG analyst (like yourself at HKU Business School CIM)


## ESG-Score-Integration (What features do I wish to include?)



## How It Works: (What did I use to measure the KPI)

KPIs that matter:
- Portfolio average ESG score, carbon intensity
- Top/bottom performers by ESG trend
- Sector ESG distribution
- Controvery Score (Risk Flags)
- Financial vs ESG correlation (P/E, EPS vs ESG score)


## Files & Data Folders: (How did I make the magic happens?)
esg-api/
├── app/
│   ├── __init__.py
│   ├── main.py              # FastAPI app entry point
│   ├── models.py            # SQLAlchemy models
│   ├── schemas.py           # Pydantic models
│   ├── database.py          # DB connection
│   ├── routers/
│   │   ├── __init__.py
│   │   ├── companies.py     # Company CRUD
│   │   ├── analytics.py     # KPI endpoints
│   └── exceptions.py        # Custom exceptions
├── scripts/
│   ├── data_import.py       # Data ingestion
│   ├── init_db.py           # Database initialization
├── tests/
│   ├── __init__.py
│   ├── test_api.py          # API tests
│   ├── test_analytics.py    # Analytics tests
├── notebooks/
│   └── data_exploration.ipynb
├── data/
│   └── .gitkeep             # Track empty folder
├── .env.example             # Example environment variables
├── .gitignore               # Ignore venv, .env, __pycache__
├── requirements.txt         # Python dependencies
├── README.md                # Setup instructions
├── API_Documentation.pdf    # Exported Swagger docs
└── Technical_Report.pdf     # Your 5-page report



## Summary
- Growth Expectation [+10 ESG score -> x1.8 EV/EBITA]
- Strategy Implementation [SLL -> (CSR) -> QoL(LT investment)]

# Steps
# 1. Open PowerShell or Command Prompt as Administrator

# 2. Navigate to your project folder
cd C:\Users\YourUsername\Documents\COMP3011
# Or wherever your project is

# 3. Create virtual environment
python -m venv venv

# 4. Activate virtual environment
.\venv\Scripts\activate

# You should see (venv) at the start of your command line

# 5. Install dependencies
pip install fastapi uvicorn sqlalchemy psycopg2-binary python-dotenv pydantic yfinance app yesg pandas pytest httpx

 6. Save installed packages
pip freeze > requirements.txt

# 7. Create .env file (use Notepad or Cursor)
# Put this content in .env:
# DATABASE_URL=postgresql://postgres:YourPostgresPassword@localhost:5432/esg_investment_db

# 8. Test database connection
python tests/test_db_connection.py

# 9. Initialize database (create tables)
python scripts/init_db.py   #   ******* Stuck here ********

# 10. Import data (takes ~5 minutes)
python scripts/data_import.py



# Install Python 3.10+ and create virtual environment
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate

# Install core dependencies
pip install fastapi uvicorn sqlalchemy psycopg2-binary pydantic python-dotenv
pip install yfinance requests pandas python-multipart
pip install pytest httpx  # for testing


### Installation
```bash
git clone https://github.com/yourusername/esg-api.git
cd esg-api
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt (Load Core dependency)

ESG Investment Search API:
curl "https://financialmodelingprep.com/stable/esg-environmental-social-governance-data?symbol=AAPL&apikey=qj0a0JowZ8d1jpQxFDjyHRfjUkGvaWIu"


# Run API

uvicorn {folder.file}:app --reload

Check file:
uvicorn app.main:app --reload
http://localhost:8000/docs

# Verify structure
tree /F app

# Check DB in PostGreSQL
\c esg_investment_db
\dt


##Error log
1.  Please correct the code of python scripts/data_import.py so that it can run correctly
Here's the output:
  File "C:\\Users\\kimch\\OneDrive\\Documents\\GitHub\\COMP3011\\esg-api\\scripts\\data_import.py", line 11, in <module>
    from app.database import SessionLocal, engine
ModuleNotFoundError: No module named 'app'
2. For Companies section of API, the post part of creating a company, is it possible to directly upload a csv for all the data (For an easier import of data rather than unfriendly csv manual input) and could we just directly request api from the yfinance for the immediate result (rather than just giving pre- trained data)
3.  For Analytics, Threre are 2 things: first, is it possible to allow the company input to be {AAPL, TSLA} (spacing and comma spacing [rather than only comma or only spacing]); Secondly, the code seems to give out a 404 Not found error code, could you maybe help me to fix that?